import { Button } from '@mui/material'

const Start = () => {
  return (
    <Button variant="contained" onClick={() => console.log('Empezamos')}>
      Empezamos
    </Button>
  )
}

export default Start
